Create your own inventory, example will be given

First ever command should be to set up the server for further commands
- 'ansible-playbook playbooks/first_setup.yml -u [server-user] -k -K' # This will setup an ansible_user, add your public ssh and setup passwordless sudo
